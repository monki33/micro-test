const _PREFIX = 'mictest'
// 登录cookie信息
export const cookieTokenKey = `${_PREFIX}TokenKey`
export const cookieUserInfoKey = `${_PREFIX}UserInfoKey`

// 本地存储中系统主题的键名
export const themeBackgroundKey = `${_PREFIX}/background`
export const themeColorKey = `${_PREFIX}/color`
