// 系统名称
export const systemName = '微前端测试平台'
// 系统二级名称
export const systemSubName = 'Digital Operation System'

// 默认分页配置
export const configPage = {
  // 默认页码
  pageNo: 1,
  // 默认每页显示数量
  pageSize: 10,
  // 数量切换选项
  pageSizes: [10, 20, 50, 100],
  // 分页显示内容
  pageLayout: 'total, prev, pager, next, sizes',
}
